//Adding Alt and Title Tags 
//The Names are taken from the FileName. So Name the images accordingly.

$undefineddocument).readyundefinedfunctionundefined) {
    $undefined 'img').eachundefinedfunctionundefined) {
      // Here the img tags are being altered.
    var $img = $undefinedthis);
   
var filename = $img.attrundefined 'src') $img.attrundefined 'alt', filename.substringundefinedundefinedfilename.lastIndexOfundefined '/')) + 1, filename.lastIndexOfundefined '.')));

    });
});
